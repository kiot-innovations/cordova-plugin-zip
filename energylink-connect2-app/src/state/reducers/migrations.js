// import { createMigrate } from 'redux-persist'

// const migrations = {}

export const migrationsMap = {}

export const storesVersions = {
  userReducer: 0,
  languageReducer: 0,
  globalReducer: 0
}
