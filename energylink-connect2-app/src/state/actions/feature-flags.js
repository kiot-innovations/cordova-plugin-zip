import { createAction } from 'redux-act'

export const UPDATE_FEATURE_FLAGS = createAction('UPDATE_FEATURE_FLAGS')
export const UPDATE_FEATURE_FLAGS_ERROR = createAction(
  'UPDATE_FEATURE_FLAGS_ERROR'
)
