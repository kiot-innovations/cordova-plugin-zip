import { createAction } from 'redux-act'

export const SHOW_MODAL = createAction('SHOW_MODAL')
export const HIDE_MODAL = createAction('HIDE_MODAL')
export const SET_CURRENT_MODAL = createAction('SET_CURRENT_MODAL')
