import React from 'react'

import { storiesOf } from '@storybook/react'

import ScrollableGridItem from './index'

storiesOf('ScrollableGridItem', module).add('Simple', () => (
  <ScrollableGridItem />
))
