import React from 'react'

import { storiesOf } from '@storybook/react'

import SocialShareButton from './index'

storiesOf('SocialShareButton', module).add('Simple', () => (
  <SocialShareButton />
))
